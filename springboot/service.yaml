apiVersion: v1
kind: Service
metadata:
  name: spring-service
spec:
  type: NodePort
  selector:
    app: spring                 # matches label in deployment
  ports:
    - port: 80                  # service port
      targetPort: 8080          # container port (Spring Boot)
      nodePort: 30080           # external port on node
